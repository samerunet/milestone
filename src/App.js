
import "./App.css";
import AppGateway from "./AppGateway";

function App() {
	return <AppGateway />;
}

export default App;

/* {CardDeck.map((card) => {
				return <img key={card.id} src={card.src} alt={card.value} />;
			})} */
